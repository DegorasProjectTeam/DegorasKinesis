# ----------------------------------------------------------------------------------------------------------------------
# EXAMPLES
#
# Each example is a self-contained subproject with its own CMakeLists.txt and main.cpp, so a future example can add
# extra sources, resources or dependencies without affecting the others. Add new examples with add_subdirectory below.
# ----------------------------------------------------------------------------------------------------------------------

add_subdirectory(basic_device_discovery)
add_subdirectory(basic_device_connection)
add_subdirectory(multi_device_control)

# **********************************************************************************************************************
